{
  "name": "schismata",
  "private": true,
  "version": "0.0.0",
  "description": "Automata & formal languages sandbox — DFA, NFA, regex, Turing machines",
  "license": "MIT",
  "packageManager": "pnpm@10.26.1",
  "scripts": {
    "dev": "pnpm --filter @schismata/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}